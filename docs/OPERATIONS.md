# 운영 안내

## 실행 환경

GitHub Actions (public repo). Vercel 무료 티어는 **불가**하다 — cron 이 하루 1회로 제한되고, 파일시스템이 휘발이라 SQLite 상태가 매 실행 사라진다. 상태가 사라지면 알림 폭탄을 내거나 아무것도 안 보내거나 둘 중 하나다.

| 워크플로 | 주기 (KST) | UTC cron |
|---|---|---|
| `check.yml` | 24시간, 30분마다 | `13,43 * * * *` |
| `digest.yml` | 매일 07:52 | `52 22 * * *` |
| `backfill.yml` | 수동 전용 | — |

`backfill` 은 스케줄이 없다. 실수로 돌면 그 시점 이후의 새 공지가 통째로 읽음 처리되어 조용히 사라지므로, 확인 문구를 입력해야 실행된다.

`--check` 는 종일 실행이라 시간대 변환이 필요 없다. **`--digest` 만 주의**하면 된다: UTC 23시가 KST 다음 날 08시다.

GitHub cron 은 부하에 따라 **최대 30분 지연**되거나 아예 걸러진다. 마감일 기준 알림이라 지연 자체는 문제되지 않지만, **정각·30분은 피한다** — 전 세계 워크플로가 몰리는 시각이라 가장 자주 걸러진다. `*/30` 으로 두었을 때 11:30Z·12:00Z 두 슬롯이 연속으로 건너뛰는 것을 실제로 관측했다(2026-08-05). 그래서 `:13/:43`, 다이제스트는 `:52` 다.

### HANDOFF §11.5 와의 차이 (의도된 변경)

명세서는 "평일 09~19시 제한"을 학교 서버 배려로 명시했다. 24시간 운영은 여기서 벗어나지만 **총 요청량은 오히려 비슷하다.**

| | 실행/일 | 목록 요청/일 | 주간 |
|---|---|---|---|
| 명세서안 (평일 10분) | 66 | 528 (평일만) | 약 2,640 |
| 현재 (24h 30분) | 48 | 384 (매일) | 약 2,688 |

요청 간격 1.5초와 병렬 금지는 그대로다. 새벽은 학교 서버가 한가한 시간이라 부하 관점에서 불리하지 않다. **이 근거를 모르고 되돌리지 말 것.**

## 시크릿

| 이름 | 필수 | 비고 |
|---|---|---|
| `GEMINI_API_KEY` | ✅ | **AI Studio 키**. 무료 한도는 여기서만 나온다 |
| `TELEGRAM_BOT_TOKEN` | ✅ | |
| `TELEGRAM_CHAT_ID` | ✅ | 채널은 `-100…` 형태. 앞자리를 빼면 조용히 실패한다 |
| `CUK_HEALTHCHECK_URL` | ✅ | `--check` 용 ping URL (모니터 `cuk-bot check`) |
| `CUK_HEALTHCHECK_DIGEST_URL` | ✅ | `--digest` 용 (모니터 `cuk-bot digest`) |
| `CUK_CONTACT_EMAIL` | 선택 | User-Agent 연락처. 현재 **미설정**(§아래) |
| `CUK_VERTEX_CREDENTIALS_JSON` | ✅ | Vertex 서비스 계정 JSON 전문. **1순위 자격증명** |

### 연락처 없이 운영 (의도된 변경)

HANDOFF §11.5 는 User-Agent 에 연락 가능한 이메일을 남기라고 한다 — 학교 전산팀이 차단 대신 문의를 보내게 하려는 장치다. 의뢰인 결정으로 **연락처 없이 운영한다**(2026-08-05). 공개 레포에 개인 이메일을 박으면 수집 대상이 되기 때문이다.

**감수하는 위험:** 학교가 이상 트래픽을 발견해도 문의할 곳이 없어 **곧바로 차단**할 수 있다. 요청량이 적고(주 2,688건) 간격이 1.5초라 가능성은 낮지만, 차단당하면 원인 파악이 늦어진다. 나중에 마음이 바뀌면 `CUK_CONTACT_EMAIL` 시크릿만 추가하면 된다.

### 자격증명 순서 (2026-08-05 변경)

**Vertex 1순위, AI Studio 2순위.** 의뢰인 결정으로 Vertex 가 부하를 지고, Vertex 가 안 되면 무료 티어가 받는다.

| 순위 | 자격증명 | 성격 |
|---|---|---|
| 1 | `CUK_VERTEX_CREDENTIALS_JSON` | **과금.** 하루 20건 제한 없음 |
| 2 | `GEMINI_API_KEY` | 무료 한도 (모델당 하루 20건) |

Vertex 가 결제 중단·키 폐기 등으로 실패하면 자동으로 AI Studio 로 넘어가고, 전환 사실이 로그에 남는다. **이 폴백이 없으면 Vertex 하나 죽었을 때 봇이 계속 돌면서 판정만 안 하는 상태**가 된다 — 전부 "판정 없이 전달"로 나가므로 겉보기엔 동작하는 것처럼 보인다.

모든 실행이 시작 시 `자격증명: ...` 을 출력한다. 과금 경로로 돌고 있는지 항상 보이게 하기 위함이다.

## 최초 기동 순서

**순서를 어기면 알림 폭탄이 난다.**

1. 시크릿 등록
2. **워크플로를 비활성 상태로 둔다** — 레포 생성 직후 스케줄이 바로 돌면 3번 전에 알림이 나간다
3. `backfill` 워크플로 수동 실행 (확인 문구 `backfill` 입력) — 기존 공지를 읽음 처리
4. 커밋된 `cuk_notices.db` 확인
5. `check` / `digest` 활성화
6. 채널 테스트 메시지 삭제

```bash
gh workflow disable check --repo <repo>   # 2번
gh workflow run backfill --repo <repo> -f confirm=backfill
gh workflow enable check --repo <repo>    # 5번
```

---

# 장애 대응

## 핵심 원칙

**침묵은 정상과 구분되지 않는다.** 봇이 죽어도 채널은 조용할 뿐이고, 이는 "새 공지 없는 날"과 똑같이 보인다. 그래서 감지 장치를 두 겹으로 둔다.

| 층 | 담당 | 신호 |
|---|---|---|
| 외부 (healthchecks.io) | "죽었는지" | ping 끊기면 이메일 |
| 내부 (`crawl_log`) | "왜 죽었는지" | 게시판별 실패 이력, git 히스토리만큼 보존 |
| 채널 | "사용자가 눈치채는지" | 매일 08:00 메시지 부재 |

**설정된 모니터** (healthchecks.io, 계정 `cnuhansa@gmail.com`)

| 모니터 | period | grace | 의미 |
|---|---|---|---|
| `cuk-bot check` | 30분 | 90분 | 3회 연속 실행이 빠지면 경고 |
| `cuk-bot digest` | 1일 | 26시간 | 아침 다이제스트가 하루 넘게 빠지면 경고 |

grace 를 주기보다 넉넉히 잡은 이유: GitHub cron 은 부하에 따라 최대 30분 지연된다. 빠듯하게 잡으면 정상 지연에도 경고가 울려 **경고를 무시하는 습관**이 생기고, 그러면 감지 장치가 있으나 마나다.

ping 은 3단계다 — 실행 시작 시 `/start`, 성공 시 기본 URL, 예외 시 `/fail`. healthchecks 가 실행 소요 시간까지 기록하므로 "돌긴 하는데 점점 느려진다" 도 보인다.

healthchecks 무료 플랜은 로그를 100건만 보존한다(30분 주기 = 약 2일). 경보 자체는 로그와 무관하므로 문제되지 않으며, 장기 추적은 `crawl_log` 가 맡는다.

## 유형별 조치

### 아침 메시지가 안 온다

가장 흔한 실제 장애 신호다.

1. GitHub Actions → 워크플로 실행 이력 확인
2. **60일 무활동으로 스케줄이 꺼졌는가?** — DB 커밋이 매 실행 쌓이므로 보통 발생하지 않지만, 봇이 오래 멈춰 있었다면 연쇄적으로 꺼질 수 있다. 수동 실행으로 되살린다
3. 실패했다면 로그에서 `자격증명:` 줄부터 확인 — 시크릿 만료가 흔한 원인

### "게시판 수집 실패" 알림이 왔다

같은 게시판이 3회 연속 실패하면 채널로 1회 통보된다(복구되면 자동 해제).

```bash
python -m cuk_bot --status          # 게시판별 실패 이력
python tools/m1_probe.py --board <id> --diagnose
```

`--diagnose` 는 본문 후보 컨테이너를 다시 순위 매긴다. 학교가 마크업을 바꿨다면 여기서 새 셀렉터가 보인다. `cuk_bot/parser.py` 의 `BODY_SELECTOR` 를 고치고 `docs/M1_RESULT.md` 를 갱신한다.

### "판정 없이 전달" 알림이 계속 온다

무료 한도가 모두 소진된 상태다. 정상 동작이며 데이터 손실은 없다.

```bash
python -m cuk_bot --status          # 모델별 잔여 확인
python -m cuk_bot --reextract       # 한도 회복 후 재판정 + 리마인더 예약
```

한도가 아니라 키 문제라면 `--status` 의 `자격증명:` 줄이 알려준다.

### 알림이 안 나간 것 같다

발송 실패분은 `outbox` 에 남아 다음 실행에서 자동 재시도된다(최대 5회).

```bash
python -m cuk_bot --status          # 미발송: 재시도 대기 N건, 재시도 포기 M건
```

"재시도 포기"가 있으면 텔레그램 설정을 확인한다. **메시지는 삭제되지 않으므로** 원인 해결 후 재시도하면 살아난다.

### 판정 규칙을 바꿨다

`normalize()` 의 규칙(마감 만료, 신뢰도 상한 등)을 고치면 **이미 저장된 판정은 낡은 채로 남는다.** 재추출은 이미 받은 답에 다시 돈을 내는 셈이라, 저장해 둔 모델 원본 응답으로 규칙만 다시 적용한다.

```bash
python -m cuk_bot --renormalize     # API 호출 없음
```

`--status` 가 낡은 판정 건수를 알려준다. 원본 응답은 `payload.raw` 에 보존되므로 규칙을 되돌려도 복구된다.

### DB가 깨졌거나 사라졌다

레포 히스토리가 곧 백업이다.

```bash
git log --oneline -- cuk_notices.db
git checkout <sha> -- cuk_notices.db
```

복구 불가능하면 **반드시 `--backfill` 을 먼저** 돌린다. 안 하면 수백 건이 한꺼번에 나간다. 단 예약돼 있던 리마인더는 사라지므로 채널에 안내한다.

### 학교에서 차단하거나 문의가 왔다

HANDOFF §13: **즉시 중단하고 의뢰인에게 보고한다.** 우회하지 않는다.

GitHub → Actions → 해당 워크플로 → `⋯` → Disable workflow

현재 User-Agent 에 연락처가 없어 학교가 문의 없이 바로 차단할 수 있다. 차단 정황이 보이면 `CUK_CONTACT_EMAIL` 등록을 검토한다.

## 비용

운영은 무료 티어(AI Studio)만 쓴다. Vertex 테스트 프로젝트의 잔여 크레딧은 **API 로 조회할 수 없어** 콘솔에서 확인해야 한다(결제 → 크레딧). 대신 `--status` 가 우리가 쓴 토큰으로 예상 비용을 추정해 보여준다 — 단가표는 수동 관리라 참고용이다.

콘솔에 월 $1 예산 알림을 걸어두길 권한다. 예산은 Budget API 로도 만들 수 있지만 서비스 계정에 결제 계정 전체 관리 권한이 필요해, 알림봇에 붙일 권한이 아니다.
