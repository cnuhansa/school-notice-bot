# 남은 작업

## 사람이 해야 하는 것 (대행 불가)

| # | 작업 | 이유 |
|---|---|---|
| H1 | **채널 테스트 메시지 3건 삭제** | 발송 시 `message_id` 를 저장하지 않았고, 봇 API 로는 채널의 과거 메시지를 조회할 수 없다. 텔레그램 앱에서 직접 삭제 |


| H4 | **Vertex 크레딧 확인 + 예산 알림** | 콘솔 전용. 결제 → 크레딧에서 잔액·만료일 확인, 월 $1 예산 알림 설정 |
| H5 | **healthchecks 기본 모니터 `My First Check` 삭제** | 가입 시 자동 생성된 샘플. ping 이 없어 **약 25시간 뒤 오탐 DOWN 경고**가 온다 |

## 코드 작업

| # | 작업 | 비고 |
|---|---|---|
| C1 | **M2 이미지 출처 14건 원문 대조** | M2 통과의 마지막 조건. 세션당 이미지 처리량 제한으로 나눠서 진행 ([M2_RESULT.md](M2_RESULT.md)) |
| C2 | **운영 키로 표본 재검증** | 테스트(Vertex)와 결과가 갈리지 않는지. 무료 한도 하루 20건이라 이틀에 나눠 실행 |
| C3 | `랭뷰 워크숍` 유형 판정 기준 | 참가 신청 여부가 본문에 불명확한 행사를 actionable 로 볼 것인가 |
| C4 | **2026-10-16 이후 모델 정리** | 체인에서 `gemini-2.5-*` 제거. 그전까지는 `ModelUnavailable` 로 자동 우회되므로 급하지 않다 |

## 확장 후보 (원본 명세서 §4.4, 미확인)

필요해지면 `BOARDS` 에 추가한다. URL 패턴은 동일할 것으로 보이나 검증하지 않았다.

- 학과 소식 `french.catholic.ac.kr/french/community/news.do`
- 취·창업처 `career.catholic.ac.kr`
- 국제교류처 `oia.catholic.ac.kr` — 프랑스어 전공 특성상 가치 높음
- 학부대학 `catholic-college.catholic.ac.kr` — 1학년인 경우

게시판을 추가하면 [CHANNEL_NOTICE.md](CHANNEL_NOTICE.md) 의 목록도 함께 고친다.

## 2차 범위로 미룬 것 (원본 명세서 §2)

- **첨부 HWP 파싱** — M1 조사 결과 기숙사 첨부는 전부 신청서 양식이라 마감일이 없다. 수익이 낮아 보류
- 로그인 필요 게시판 (트리니티·사이버캠퍼스)
- 웹 UI / 다중 사용자 / 카카오톡 발송
